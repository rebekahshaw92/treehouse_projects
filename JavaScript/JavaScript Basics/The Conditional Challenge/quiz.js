var correctAnswer = false;

var firstQuestion = prompt("What is the name of the German airline?");

var firstAnswer = firstQuestion.toLocaleUpperCase();


if (firstAnswer === "LUFTHANSA") {
correctGuess = true;
  alert("you are right");

} //else if ( parseInt(guess) < randomNumber ) {
  //var guessMore = prompt('Try again. The number I am thinking of is more than ' + guess);
  //if (parseInt(guessMore) === randomNumber) {
    //  correctGuess = true;
  //}


var secondQuestion = prompt("In which European city can you find the home of Anne Frank?");

var thirdQuestion = prompt("How many stars has the American flag got?");

var fourthQuestion = prompt("According to the Bible, who was the first murderer?");

var fifthQuestion = prompt("Which animal is on the golden Flemish flag?");